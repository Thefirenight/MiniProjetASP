
@{
    Layout = "/Views/Shared/_Layout.cshtml";
    ViewBag.Title = "WebNovels";
    @model Test.Models.DataModel.Rootobject
}
<div class="container table-responsive-sm">
    <table class="table table-hover table-condensed">
        <caption>Liste des WebNovels</caption>
        <thead class="thead-light">
            <tr>
                <th scope="col">ID</th>
                <th scope="col" colspan="3">Nom</th>
                <th scope="col" colspan="5">Description</th>
            </tr>
        </thead>
        <tbody>
            @{
                foreach (var wn in Model.listWebNovels)
                {
                    <tr>
                        <th scope="row">@wn.ID</th>
                        <td colspan="3">@wn.Name</td>
                        <td colspan="5">@wn.Description</td>
                        <td><a class="btn btn-info" href="@Url.Action("wnDetails", "Detail", new { id = wn.ID })" role="button">+Info</a></td>
                    </tr>

                }
            }
        </tbody>
    </table>


</div>